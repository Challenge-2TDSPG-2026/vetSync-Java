spring.application.name=VetSync

# ===== Banco de dados: Oracle FIAP =====
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:oracle:thin:@oracle.fiap.com.br:1521:ORCL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:RM562085}
spring.datasource.password=${DB_PASSWORD:}
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.OracleDialect

# Pool de conexões Hikari (limitado para respeitar a cota SESSIONS_PER_USER do Oracle FIAP)
spring.datasource.hikari.maximum-pool-size=2
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.max-lifetime=60000
spring.datasource.hikari.connection-timeout=20000

spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.open-in-view=true

spring.flyway.enabled=${SPRING_FLYWAY_ENABLED:true}
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true
spring.flyway.validate-migration-naming=true

spring.cache.type=simple

management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always

jwt.secret=${JWT_SECRET}
jwt.expiration=86400000

spring.mail.host=${MAIL_HOST}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

app.mail.remetente=${MAIL_USERNAME}

app.admin.bootstrap-key=${ADMIN_BOOTSTRAP_KEY}

# Carteira de vacinação compartilhável (QR Code / link público)
app.carteira-compartilhada.expiracao-dias=${CARTEIRA_COMPARTILHADA_EXPIRACAO_DIAS:30}

# Convite de acesso (cuidador/cônjuge) a um pet
# WEB_INVITE_URL: página do site que trata "?token=..." (GET /auth/convites/{token} + POST /auth/registrar-convite)
app.convite.web-invite-url=${WEB_INVITE_URL:https://vetsync-theta.vercel.app/convite}
app.convite.expiracao-horas=${PET_CONVITE_EXPIRACAO_HORAS:72}

# Origem do site web liberada no CORS (além de localhost/emulador). Aceita múltiplas, separadas por vírgula.
app.cors.web-origin=${APP_CORS_WEB_ORIGIN:}

server.port=8080

# Upload de imagens (ex.: imagem de produto no catálogo de recompensas)
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB
